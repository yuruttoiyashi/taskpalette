# TaskPalette 業務依頼モード追加

## 今回の追加機能

1. 「業務依頼」入力モードを追加
2. 1つの依頼文から複数タスクを抽出
3. 「確認後」「完了後」「問題がなければ」「〜してから」などの明示的な前後関係から前提タスクを判定
4. 担当者または期限がない場合に「要確認」を表示
5. 前提タスクをD1へ保存し、タスク一覧でも保持
6. AIが存在しない前提タスクを作った場合は、実際に抽出されたタスクと照合して除外
7. Workers AIが利用できない場合の簡易解析でも、代表的な業務依頼を複数タスクへ分解
8. 「明日」「明後日」「月末」「来月末」などの相対期限を日本時間で解析

## デプロイ前に必要な操作

この更新では tasks テーブルへ prerequisite 列を追加します。
必ずコードのデプロイより先にD1マイグレーションを実行してください。

PowerShellでプロジェクトフォルダを開き、以下を順番に実行します。

npm run db:migrate:remote

成功後:

npm run deploy

## 動作確認用サンプル

文章の種類を「業務依頼」に変更して「サンプルを入力」を押すと、
今回の機能を確認できる文章が自動入力されます。

期待される主な結果:
- 売上資料を確認する / 担当: 田中さん
- 売上資料を部長へ送付する / 前提: 売上資料を確認する
- 比較表を作成する / 担当者: 要確認
- 取引先へ共有する / 担当者・期限: 要確認
