# TaskPalette レイアウト修正（v1.0.1）

## 修正内容

- 1366px前後でタスク表が返信文・AI整理ログへ重なる問題を修正
- 1366px前後ではタスク表を横幅いっぱいにし、返信文とログをその下に2列で表示
- サマリーカードを中間幅で2列×2段に変更し、縦長になりすぎる問題を修正
- タスク表の各列に `min-width: 0` を設定し、長文によるはみ出しを防止
- 760px以下ではタスク行をカード型に変更
- 430px以下のスマートフォン向け表示を調整

## 既存プロジェクトへ反映する場合

`src/styles.css` だけを修正版で上書きしてください。
D1の設定やデータベースIDを変更する必要はありません。

```powershell
npm run cf:dev
```
