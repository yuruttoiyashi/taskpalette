# AI整理の「未保存 → 保存」方式への変更

## 変更後の動き

1. 「AIで整理する」を押すと、結果は画面上の未保存データとして表示されます。
2. 未保存のままもう一度AI整理すると、前回の未保存結果は追加されず、新しい結果に置き換わります。
3. 「○件を保存」を押した時だけ、解析履歴・タスク・返信文がD1に保存されます。
4. 保存前のタスクは編集でき、編集内容をそのまま保存できます。
5. サマリーやタスク一覧は、保存が完了するまで増えません。

## 主な変更ファイル

- `src/App.tsx`
- `src/api.ts`
- `src/types.ts`
- `src/styles.css`
- `worker/index.ts`

## D1について

今回の修正ではテーブル構成を変更していないため、新しいマイグレーションは不要です。
既にD1へ保存されている重複タスクは自動削除しません。

## デプロイ

依存関係が未インストールなら最初に:

```bash
npm install
```

その後:

```bash
npm run deploy
```
