# TaskPalette 8/18 ドラッグ＆ドロップ実行順アップデート

## 追加機能
- AIが提案した「実行順」をドラッグ＆ドロップで手動変更
- 左側のグリップハンドルから並べ替え
- ドロップ位置を上/下のラインで表示
- 保存前のAI整理結果は、変更した順番のままD1へ保存
- 保存済みのタスク一覧でも実行順をドラッグ＆ドロップで変更
- 保存済みタスクの並び替えはD1へ永続保存
- 前提タスクより後続タスクを前へ移動しようとした場合は並べ替えを拒否
- 検索・絞り込み中は保存済みタスクの並べ替えを無効化し、全件表示時のみ安全に利用

## DB変更
migrations/0005_execution_order.sql を追加しています。
tasks テーブルに execution_order 列を追加します。

## デプロイ手順
PowerShellで tp_taskfix フォルダを開き、以下を順番に実行してください。

npm run db:migrate:remote

成功したら:

npm run deploy

## 動作確認
1. AI整理で業務依頼を生成
2. 並び順を「実行順」にする
3. 各タスク左側のグリップをドラッグ
4. 実行1、実行2…の順番が変わることを確認
5. 「○件を保存」でD1へ保存
6. タスク一覧の「実行順」でも順番が維持されていることを確認
7. 前提タスクのある後続タスクを前提より前へ移動し、警告されることを確認
