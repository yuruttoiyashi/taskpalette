# 担当者抽出の改善

- AIの `title` から「山田さんは、」「佐藤さんが、」のような担当者表現を分離し、`assignee` へ移動
- 同じ段落内で担当者が継続する文（例: 「山田さんはAする。あわせてBする。」）は、Bにも山田さんを引き継ぐ
- 「山田さんへ共有」のような宛先・共有先は、実行担当者として扱わない
- AIが `assignee` を未設定で返しても、元文章との類似度から担当者文脈を補完
- 決定事項だけの文を不要にタスク化しないようAIプロンプトも調整

D1のスキーマ変更はありません。マイグレーション不要です。
